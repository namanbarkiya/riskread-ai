# =====================================================
# ENVIRONMENT VARIABLES FOR RISKREAD AI
# =====================================================
# Copy this file to .env.local and fill in your values
# =====================================================

# Core configuration
#
# Copy this file to `.env` (or `.env.local`) and fill in the values.

# Gemini AI Configuration (server-side only; never expose these to the client)
GEMINI_API_KEY=<your_gemini_api_key>
GEMINI_MODEL=gemini-2.5-flash

# Application Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000
NODE_ENV=development

# Analysis polling configuration (client-side)
# Polling interval in milliseconds for analysis status updates (default: 5000)
NEXT_PUBLIC_ANALYSIS_POLL_INTERVAL_MS=5000

# Optional: Analytics and Monitoring
NEXT_PUBLIC_ANALYTICS_ID=<your_analytics_id>
NEXT_PUBLIC_SENTRY_DSN=<your_sentry_dsn>